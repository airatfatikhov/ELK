input {
  beats {
    port => 5044
  }

  tcp {
    port => 5000
    codec => json
  }
}

filter {
  # Здесь можно добавить grok, mutate, date, geoip и т.д.
}

output {
  elasticsearch {
    hosts => [
      "http://es-data-1:9200",
      "http://es-data-2:9200",
      "http://es-data-3:9200"
    ]
    index => "logs-%{+YYYY.MM.dd}"
  }

  stdout {
    codec => rubydebug
  }
}